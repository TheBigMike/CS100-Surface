#include <stdio.h>

int
main(int argc, char **argv)
    {
    int a = 1, b = 2, c = 3, d = 4;
    int w, x, y, z;
    w = i(d);
    x = h(c,w);
    y = g(b,x);
    z = f(a,y);
    printf("result is %d\n",z);
    return 0;
    }
