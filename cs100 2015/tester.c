//template for testing code
//download with: wget troll.cs.ua.edu/ACP-C/tester.c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>

#include "scanner.h"

/*int giveMax(a,b)
    {
    if (a > b)
        return a;
    else if (b < a)
        return b;
    }

int main(int argc,char **argv)
    {
    int a;
    int b;
    FILE *fp;

    fp = fopen(argv[1],"r");
    
    //printf("Give me a number: ");
    //scanf("%d", &a);
    a = readInt(fp);
    //printf("Give me another number: ");
    //scanf("%d", &b);
    b = readInt(fp);
    printf("The larger number is %d.", giveMax(a,b));
    fclose(fp);

int main()
    {
    double val1;
    double val2;
    double val3;
    double val4;
    double val5;
    double val6;
    double val7;
    double val8;
    double val9;
    double val10;
    double sum;
    double average;
    printf("Give me ten values with spaces between them: ");
    scanf("%lf %lf %lf %lf %lf %lf %lf %lf %lf %lf",&val1, &val2, &val3, &val4, &val5, &val6, &val7, &val8, &val9, &val10);
    sum = val1 + val2 + val3 + val4 + val5 + val6 + val7 + val8 + val9 + val10;
    average = sum / 10;
    printf("The average is %f",average);

    
*/
    
int main()
    {
    //int i;
    //double sum = 0;
    //for(i=0; i<10; ++i)
    //    {
    //    double x;
        
    //    printf("Give me a value: ");
    //    scanf("%lf",&x);
    //   sum = x + sum;
        
    //    }
    //printf("The average is %lf", sum / 10); 
    i = 0;
    while (i < n)
        {
        printf("kek");
        }
    return 0;
    }
